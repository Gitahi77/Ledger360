'use client';

// TemporalEngine is intentionally removed — it duplicated the PeriodSelector.
// This empty export keeps imports that may still reference it from breaking.
export function TemporalEngine() { return null; }
